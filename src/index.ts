import { Magpie } from "./magpie";

/**
 * The main Magpie SDK client class.
 * 
 * This is the primary entry point for the Magpie SDK, providing access to all
 * payment processing functionality including charges, customers, sources,
 * checkout sessions, and more.
 * 
 * Exported as both default and named export for maximum compatibility with
 * different import styles (ES modules, CommonJS, TypeScript).
 */
export default Magpie;
export { Magpie };
